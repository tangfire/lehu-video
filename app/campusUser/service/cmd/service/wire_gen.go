// Code generated by Wire. DO NOT EDIT.

//go:generate go run -mod=mod github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"github.com/go-kratos/kratos/v2"
	"github.com/go-kratos/kratos/v2/log"
	"lehu-video/app/campusUser/service/internal/biz"
	"lehu-video/app/campusUser/service/internal/conf"
	"lehu-video/app/campusUser/service/internal/data"
	"lehu-video/app/campusUser/service/internal/server"
	"lehu-video/app/campusUser/service/internal/service"
)

import (
	_ "go.uber.org/automaxprocs"
)

// Injectors from wire.go:

// wireApp init kratos application.
func wireApp(confServer *conf.Server, idgen *conf.Idgen, registry *conf.Registry, confData *conf.Data, logger log.Logger) (*kratos.App, func(), error) {
	db, err := data.NewDB(confData, logger)
	if err != nil {
		return nil, nil, err
	}
	client, err := data.NewRedis(confData)
	if err != nil {
		return nil, nil, err
	}
	dataData, cleanup, err := data.NewData(db, client, logger)
	if err != nil {
		return nil, nil, err
	}
	userRepo := data.NewUserRepo(dataData, logger)
	userCounterRepo := data.NewUserCounterRepo(client, logger)
	generator := data.NewIdGenerator(idgen)
	userUsecase := biz.NewUserUsecase(userRepo, userCounterRepo, generator, logger)
	userServiceService := service.NewUserServiceService(userUsecase)
	grpcServer := server.NewGRPCServer(confServer, userServiceService, logger)
	httpServer := server.NewHTTPServer(confServer, logger)
	registrar := server.NewRegistrar(registry)
	app := newApp(logger, grpcServer, httpServer, registrar)
	return app, func() {
		cleanup()
	}, nil
}
